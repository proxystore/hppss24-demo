[app]
name = "synthetic"
structure = "bag"
task_count = 10000
task_data_bytes = 1000000
task_sleep = 0
# Should be set in CLI to number of workers
# bag_max_running = 1

[engine.executor]
name = "dask"
scheduler = "localhost:8786"

[engine.filter]
name = "object-type"
patterns = ["Data"]
